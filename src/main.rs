mod exploration;

fn main() {
    pretty_env_logger::init();

    // examples::high_level_example();
    // examples::boolean();
    // examples::shortint();
    // exploration::integer();
    exploration::shortint_array();
}
